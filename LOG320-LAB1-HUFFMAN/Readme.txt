Fonctionne du programme:

Pour compresser:
    compress -compress <fichier>

    Le résultat est un nouveau fichier avec le nom 'fichier'.huf.

Pour décompresser
    compress -decompress <fichier.huf>

    Le résultat est un nouveau fichier dont le nom est 'fichier'
